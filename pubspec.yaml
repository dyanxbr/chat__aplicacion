name: chat__aplicacion
description: "A&F Chat"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.2
  image_picker: ^1.1.2
  file_picker: ^8.0.0
  url_launcher: ^6.3.0
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
  flutter_local_notifications: ^17.0.0
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.0.0

  # 👇 AGREGA ESTAS
  camera: ^0.10.5+5
  google_mlkit_face_detection: ^0.9.0
  google_mlkit_commons: ^0.6.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true